import * as React from "react";
import { Route, Switch, BrowserRouter as Router, Link } from "react-router-dom";

const routes: JSX.Element = (
  <Switch>{/* <Route path="/path" component={ComponentName} /> */}</Switch>
);

export default routes;
